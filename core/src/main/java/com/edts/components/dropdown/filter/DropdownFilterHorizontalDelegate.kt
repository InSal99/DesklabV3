package com.edts.components.dropdown.filter

import com.edts.components.dropdown.filter.DropdownFilterHorizontal

/**
 * Delegate interface to handle click events from the CustomDropdownFilterSelection component.
 */
interface DropdownFilterHorizontalDelegate {
    fun onClick(dropdown: DropdownFilterHorizontal)
}
